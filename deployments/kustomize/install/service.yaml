kind: Service
apiVersion: v1
metadata:
  name: gregor-hires-project-webapi
spec:
  selector:
    pod: gregor-hires-project-webapi-label
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: webapi-port